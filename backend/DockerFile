FROM node:20-alpine

# wget is used by the Docker healthcheck
RUN apk add --no-cache wget

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 8000

ENTRYPOINT ["sh", "./entrypoint.sh"]
